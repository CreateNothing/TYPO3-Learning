<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}TYPO3 Learning Platform{% endblock %}</title>
        <style>
            :root {
                color-scheme: light dark;
                font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
                background-color: #0f172a;
                color: #e2e8f0;
            }
            body {
                margin: 0;
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                background: radial-gradient(circle at top, rgba(59, 130, 246, 0.35), transparent 55%),
                    radial-gradient(circle at bottom, rgba(16, 185, 129, 0.25), transparent 60%),
                    #0f172a;
            }
            a {
                color: inherit;
                text-decoration: none;
            }
            .navbar {
                display: flex;
                align-items: center;
                justify-content: space-between;
                padding: 1rem 2.5rem;
                background: rgba(15, 23, 42, 0.75);
                backdrop-filter: blur(10px);
                border-bottom: 1px solid rgba(226, 232, 240, 0.1);
            }
            .navbar__brand {
                font-weight: 600;
                font-size: 1.1rem;
                letter-spacing: 0.05em;
            }
            .navbar__links,
            .navbar__auth {
                display: flex;
                gap: 1rem;
                align-items: center;
                font-size: 0.95rem;
            }
            .navbar a {
                padding: 0.35rem 0.75rem;
                border-radius: 0.6rem;
                transition: background 0.2s ease, color 0.2s ease;
            }
            .navbar a:hover,
            .navbar a:focus-visible {
                background: rgba(59, 130, 246, 0.2);
                outline: none;
            }
            .container {
                flex: 1;
                width: min(960px, 90vw);
                margin: 3rem auto 2.5rem;
                background: rgba(15, 23, 42, 0.6);
                border: 1px solid rgba(14, 116, 144, 0.3);
                border-radius: 1.25rem;
                padding: 3rem;
                box-shadow: 0 25px 70px rgba(15, 23, 42, 0.6);
            }
            .container h1 {
                margin-top: 0;
                font-size: clamp(2.25rem, 3vw + 1rem, 3rem);
                font-weight: 700;
            }
            .container p {
                color: rgba(226, 232, 240, 0.85);
                line-height: 1.6;
            }
            .cta {
                margin-top: 2.5rem;
                display: inline-flex;
                gap: 0.75rem;
                align-items: center;
                padding: 0.85rem 1.4rem;
                border-radius: 0.9rem;
                background: linear-gradient(135deg, rgba(59, 130, 246, 0.9), rgba(16, 185, 129, 0.9));
                color: #0f172a;
                font-weight: 600;
                transition: transform 0.2s ease;
            }
            .cta:hover {
                transform: translateY(-2px);
            }
            .pill {
                display: inline-block;
                padding: 0.35rem 0.9rem;
                border-radius: 9999px;
                background: rgba(59, 130, 246, 0.2);
                border: 1px solid rgba(59, 130, 246, 0.35);
                font-size: 0.8rem;
                letter-spacing: 0.08em;
                text-transform: uppercase;
            }
            footer {
                padding: 1.5rem 0;
                text-align: center;
                color: rgba(148, 163, 184, 0.85);
                font-size: 0.85rem;
            }
            @media (max-width: 720px) {
                .navbar {
                    flex-wrap: wrap;
                    gap: 1rem;
                    padding: 1rem 1.5rem;
                }
                .container {
                    padding: 2.5rem 1.5rem;
                    margin: 2.25rem auto;
                }
            }
        </style>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar">
            <a class="navbar__brand" href="{{ path('app_home') }}">TYPO3 Learning Platform</a>
            <div class="navbar__links">
                <a href="{{ path('app_home') }}">Home</a>
                <a href="{{ path('app_battle') }}">Battle</a>
                <a href="{{ path('app_learning') }}">Learning</a>
            </div>
            <div class="navbar__auth">
                {% if app.user %}
                    <span class="pill">{{ app.user.handle ?? app.user.userIdentifier }}</span>
                    <a href="{{ path('app_logout') }}">Logout</a>
                {% else %}
                    <a href="{{ path('app_login') }}">Login</a>
                {% endif %}
            </div>
        </nav>
        <main class="container">
            {% block body %}{% endblock %}
        </main>
        <footer>
            &copy; {{ "now"|date('Y') }} TYPO3 Learning Platform — Crafted with Symfony 7 & Twig.
        </footer>
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </body>
</html>
